import { FC } from 'react';
import { Heading } from '@chakra-ui/react';

export default () => (
	<Heading as='a' fontSize={['1.8rem', '2rem']} href='/' cursor='pointer'>
		ACME
	</Heading>
);
